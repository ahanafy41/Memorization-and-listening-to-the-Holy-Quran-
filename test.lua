-- شركة البرمجيات المستقلة - وحدة اختبار المنطق
print("🧪 بدء اختبارات الوحدات (Unit Tests)...")

local function test_normalization()
    print("  - فحص دالة معالجة النصوص...")
    -- سنقوم بمحاكاة منطق البحث هنا مستقبلاً
    return true
end

if test_normalization() then
    print("✅ جميع الاختبارات المنطقية مرت بسلام.")
else
    print("❌ فشل في الاختبارات المنطقية!")
    os.exit(1)
end
